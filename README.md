# portfolio

Le site (frontend React + Vite) est dans le dossier `web/`.

## Démarrage

```bash
cd web
npm install
npm run dev
```

## Build

```bash
cd web
npm run build
```

## Lint

```bash
cd web
npm run lint
```
